#include <stdio.h>
#include <stdlib.h>

int main()
{
	return 0;
}
float racine(int a, float precision)
{
	float res;
	float max;
	float min;
	int i;
	
	max = a / 2.0;
	min = 1;
	res = (max + min) / 2.0
	while(a - res * res > precision)
	{
		if(res < a * a)
	}
}


//quand s'arrêter. quand (a - res * res) < precision
